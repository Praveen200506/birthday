import SurpriseSection from "@/components/SurpriseSection";

export default function SurprisePage() {
    return (
        <main className="min-h-screen flex items-center justify-center bg-warm-cream w-full max-w-full overflow-x-hidden">
            <SurpriseSection />
        </main>
    );
}
