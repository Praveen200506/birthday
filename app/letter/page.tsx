import BirthdayLetter from "@/components/BirthdayLetter";

export default function LetterPage() {
    return (
        <main className="min-h-screen flex items-center justify-center bg-lavender/30 w-full max-w-full overflow-x-hidden">
            <BirthdayLetter />
        </main>
    );
}
